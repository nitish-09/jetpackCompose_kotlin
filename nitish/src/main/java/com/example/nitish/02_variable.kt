package com.example.nitish

// in kotlin variable is declared using 'var' and 'val' keyword
// var is mutable variable. mutable means, we can change the value of varaible
// val is immutabe varaiable. mutable means, we can't change the value of varaible

fun main() {
    var language="Kotlin"  // if we write anything in double quotes kotlin understand that, this is a string
    val language1="java"
    //language1="C"    // it gives error

    var salary: Int  // variable can also intialize later on when the progarm is executed
    salary=400
}